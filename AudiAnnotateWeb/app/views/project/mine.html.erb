<h1>AudiAnnotate</h1>
<p>Splash text about AA</p>
<h2>My Projects</h2>
<div>
  <% @repos.each do |repo| %>
    <div>
      <!-- Name, owner, link to jekyll home. -->
      <%= link_to(repo.name, project_path(repo.owner.login, repo.name)) %>
  <% end %>
</div>  

<%= link_to 'New Project', new_project_path %>

